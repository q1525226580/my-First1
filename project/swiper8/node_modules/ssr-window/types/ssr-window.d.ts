import { getDocument, ssrDocument } from 'ssr-window/types/document';
import { getWindow, ssrWindow } from 'ssr-window/types/window';
import extend from 'ssr-window/types/extend';
export { getDocument, ssrDocument, getWindow, ssrWindow, extend };
